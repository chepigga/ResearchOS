# Legacy XAU code

The supplied `Grok_Core_XAU.mq5` is classified as a legacy AI-driven EA and is not the frozen MorrisCandle V2 / BH_SWEEP oracle.

The original file contained a plaintext xAI API key, so the secret-bearing source is intentionally not committed. Provenance hashes and the security finding are recorded in:

- `../../DataIndex/BH_OOS_001v2_inputs.md`
- `../../Security/SEC-001_plaintext_xai_key.md`

A redacted copy may be committed later only if it becomes necessary for historical comparison.
