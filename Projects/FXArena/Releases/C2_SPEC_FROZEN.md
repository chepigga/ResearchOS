# C2 SPEC — FROZEN 2026-07-20, ДО фіту з економічним label
# (жодного фіту на економічному label до цього моменту не виконувалось)

ЛЕЙБЛ (нове, єдина зміна проти S2R#1):
 y = (net_R > 0) з universe_outcomes.pkl (291 659 precomputed
 реплеїв: вхід D3+60, SL/TP канон-геометрія, той самий виконавець).
 Захист від label-leakage: у train місяця M входять ТІЛЬКИ епізоди
 з exit_t < перший D3 тест-місяця (120-хв горизонт -> межа чиста).

ФІЧІ: рівно ті самі 48 (31 raw + 9 derived + 8 t) — без змін.
МОДЕЛЬ: LogisticRegression C=0.5 + StandardScaler, monthly-WF <M.

СЕЛЕКЦІЯ (pre-declared; калібрування нового p_win апріорі невідоме,
тому rank-based, потік = канонному):
 щомісяця top 4.0% епізодів за p_win (це ~= канонний кандидатний
 потік p_failed<0.20 = 3.9-4.0%), далі незмінний risk-шар:
 chrono day-cap 6 + loss-cooldown 2->12h.

ВИКОНАВЕЦЬ: векторний universe-реплей для G1-G4/G6; при PASS —
 фінальне підтвердження loop-реплеєром (еталонним).

СУДДЯ (same-executor реф: EV +0.281 | PF 2.07 | DD 15.0R | 1/40 | -3.1R):
 G1 EV_net >= +0.22R
 G2 MaxDD <= 16R
 G3 мінусових місяців <= 2/42, жоден < -6R
 G4 всі 4 роки EV > 0
 G5 AC7 rev-chrono (train пізні -> тест ранні) підтверджує G1-G2
 G6 permutation: 200 шафлів p у межах місяця, повний pipeline;
    реальний EV > p95 null
ОДИН прогін. FAIL -> аналіз, БЕЗ підгонки, наступна гіпотеза = нова
спека нова сесія. ФІНАЛЬНИЙ суддя: серпень-2026 untouched форвард.

ЗАБОРОНИ: незмінні (defender; збіг-із-Signals як метрика; рух
селекційного параметра після цього документа; режимні перемикачі).
