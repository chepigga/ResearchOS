# FXArena Results

Use `Results/<LabName>/<Version>/`. Every version directory requires a README describing producer, code, inputs, date, parameters, primary files, diagnostics, canonical status, and lineage.
